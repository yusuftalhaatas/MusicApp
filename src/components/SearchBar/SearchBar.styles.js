import { StyleSheet } from "react-native"

export default StyleSheet.create({
    container: {
        padding: 10,
        margin: 5,
        borderRadius: 5,
        backgroundColor: "#eceff1",
        height: 40,
        marginHorizontal: 10
    },
    searchTitle: {
        color: "black",
    }
})